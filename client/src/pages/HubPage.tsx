import { ProfileGate } from "@/app/ProfileGate";
import { AppFrame, RoomBadge } from "@/app/AppFrame";
import { useWorkspace } from "@/app/WorkspaceProvider";
import { ArrowUpRight, FilePlus2, Link2, ShieldCheck, Trash2 } from "lucide-react";
import { useEffect, useState } from "react";
import { useLocation } from "wouter";
import { trpc } from "@/lib/trpc";
import "./guest-room-access.css";

export function HubPage() {
  const [, navigate] = useLocation(); const { documents, loading, createDocument, removeDocument, profile } = useWorkspace(); const [roomCode, setRoomCode] = useState(""); const [joinError, setJoinError] = useState(""); const liveRooms = trpc.room.liveCount.useQuery(undefined, { refetchInterval: 15000 });
  useEffect(() => { if (window.matchMedia("(prefers-reduced-motion: reduce)").matches || !("IntersectionObserver" in window)) return; const observer = new IntersectionObserver(entries => entries.forEach(entry => { if (entry.isIntersecting) { entry.target.classList.add("hub-reveal-visible"); observer.unobserve(entry.target); } }), { threshold: .14 }); document.querySelectorAll(".hub-reveal").forEach(element => observer.observe(element)); return () => observer.disconnect(); }, []);
  const create = async () => { const document = await createDocument(); navigate(`/studio/${document.id}`); };
  const join = (event: React.FormEvent) => { event.preventDefault(); setJoinError(""); if (!/^[A-Z0-9]{8}$/i.test(roomCode)) { setJoinError("Enter the eight-character code from a real room invite."); return; } navigate(`/r/${roomCode.toUpperCase()}`); };
  const tiltHero = (event: React.PointerEvent<HTMLElement>) => { if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return; const bounds = event.currentTarget.getBoundingClientRect(); event.currentTarget.style.setProperty("--hub-pointer-x", `${((event.clientX - bounds.left) / bounds.width - .5) * 10}px`); event.currentTarget.style.setProperty("--hub-pointer-y", `${((event.clientY - bounds.top) / bounds.height - .5) * 8}px`); };
  const resetHero = (event: React.PointerEvent<HTMLElement>) => { event.currentTarget.style.setProperty("--hub-pointer-x", "0px"); event.currentTarget.style.setProperty("--hub-pointer-y", "0px"); };
  return <ProfileGate><AppFrame><div className="hub-layout"><section className="hub-intro hub-reveal hub-hero-depth" onPointerMove={tiltHero} onPointerLeave={resetHero}><div><p className="eyebrow">Your workspace · {liveRooms.data ?? 0} live rooms</p><h1><span>Documents</span><br />without a cloud.</h1><p>Every draft starts in this browser. A room is created only through the verified room registry, then its document syncs directly between approved peers.</p></div><div className="hub-actions"><button onClick={() => void create()}><FilePlus2 size={18} />Start a note <ArrowUpRight size={15} /></button><button onClick={() => document.getElementById("join-room")?.scrollIntoView({ behavior: "smooth", block: "center" })}><Link2 size={18} />Join a room <ArrowUpRight size={15} /></button></div><i className="hub-orbit hub-orbit-a" aria-hidden="true" /><i className="hub-orbit hub-orbit-b" aria-hidden="true" /></section><section className="document-library hub-reveal"><header><div><p className="eyebrow">Local library</p><h2>Open work</h2></div><span>{String(documents.length).padStart(2, "0")} items</span></header>{loading ? <p className="loading">Opening local index…</p> : documents.length ? <div className="document-grid">{documents.map((document, index) => <article key={document.id} className="hub-card-reveal" style={{ "--card-delay": `${index * 45}ms` } as React.CSSProperties}><button className="document-card-main" onClick={() => navigate(`/studio/${document.id}`)}><span className="document-card-type">{document.roomId ? (document.roomProtected ? "Password room" : "Open room") : "Private draft"}</span><h3>{document.title}</h3><p>Updated {new Date(document.updatedAt).toLocaleDateString()}</p><RoomBadge connected={Boolean(document.roomId)} /><ArrowUpRight size={18} /></button><button className="delete-note" onClick={() => { if (confirm(`Remove “${document.title}” from this browser?`)) void removeDocument(document.id); }} aria-label={`Remove ${document.title}`}><Trash2 size={15} /></button></article>)}</div> : <div className="empty-library"><span>Nothing saved here yet.</span><h3>Start with a single private thought.</h3><button onClick={() => void create()}>Create first note <ArrowUpRight size={16} /></button></div>}</section><section id="join-room" className="join-room join-room-flow hub-reveal"><div className="join-room-copy"><p className="eyebrow">Verified room entry</p><h2>Enter the room code.</h2><p>We first verify that the room exists. If its owner enabled a password, we’ll ask for it in the protected next step—never here.</p><ol className="join-room-steps"><li className="active"><span>01</span> Verify room code</li><li><span>02</span> Confirm protection</li><li><span>03</span> Request approval</li></ol></div><form onSubmit={join}><label htmlFor="room-code">Room code<input id="room-code" value={roomCode} onChange={event => { setRoomCode(event.target.value.replace(/[^a-z0-9]/gi, "").toUpperCase()); setJoinError(""); }} maxLength={8} inputMode="text" autoCapitalize="characters" autoComplete="off" placeholder="AB12CD34" aria-describedby="room-code-help" /></label><p id="room-code-help" className="join-room-help"><ShieldCheck size={15} />Password checks happen only after the room code is verified.</p>{joinError && <p className="join-error" role="alert">{joinError}</p>}<button type="submit" disabled={!profile}>Continue securely <ArrowUpRight size={16} /></button></form></section></div></AppFrame></ProfileGate>;
}
