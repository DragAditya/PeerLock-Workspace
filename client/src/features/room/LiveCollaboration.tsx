import type { PeerPresence, RoomMessage } from "@/features/editor/usePeerDocument";
import { Check, MessageCircleMore, Send, SmilePlus, UsersRound, X } from "lucide-react";

const reactionOptions = ["👍", "❤", "😂", "🎉"] as const;

export function LiveCollaboration({ peers, chat, message, setMessage, send, react, currentIdentityId, requests = [], decideRequest, deciding }: { peers: PeerPresence[]; chat: RoomMessage[]; message: string; setMessage: (value: string) => void; send: (value: string) => void; react: (messageId: string, emoji: string) => void; currentIdentityId?: string; requests?: Array<{ id: string; name: string; color: string }>; decideRequest?: (requestId: string, allow: boolean) => void; deciding?: boolean }) {
  return <section className="studio-live"><div className="studio-live-head"><div><p className="eyebrow">Live collaboration</p><h2>People and conversation, always in view.</h2></div><span><UsersRound size={15} />{peers.length} active</span></div><div className="studio-live-grid"><div className="live-peers"><p className="eyebrow">In this document</p><div className="live-peer-avatars">{peers.length ? peers.slice(0, 10).map(peer => <span className="live-peer-avatar" key={peer.id} title={peer.name}>{peer.avatarUrl ? <img src={peer.avatarUrl} alt={`${peer.name} profile`} /> : <i style={{ backgroundColor: peer.color }}>{peer.name.slice(0, 1).toUpperCase()}</i>}</span>) : <span className="live-empty">Open a room to meet peers here.</span>}</div>{peers.length ? peers.map(peer => <span className="live-peer-name" key={`name-${peer.id}`}>{peer.avatarUrl ? <img className="peer-avatar" src={peer.avatarUrl} alt="" /> : <i style={{ backgroundColor: peer.color }} />}{peer.name}</span>) : null}</div><div className="live-chat">{requests.length ? <section className="live-join-requests" aria-label="Pending room join requests"><header><p className="eyebrow">Join requests</p><span>{requests.length} waiting</span></header>{requests.map(request => <article key={request.id}><div className="join-request-person"><i style={{ backgroundColor: request.color }} /><div><b>{request.name}</b><span>Requesting secure room access</span></div></div><div className="join-request-actions"><button className="join-allow" disabled={deciding} onClick={() => decideRequest?.(request.id, true)} aria-label={`Allow ${request.name}`}><Check size={14} />Allow</button><button className="join-decline" disabled={deciding} onClick={() => decideRequest?.(request.id, false)} aria-label={`Decline ${request.name}`}><X size={14} />Decline</button></div></article>)}</section> : null}<header><MessageCircleMore size={16} /><b>Room chat</b></header><div className="live-chat-log">{chat.length ? chat.slice(-8).map(item => <article className="room-chat-message" key={item.id}><div className="room-chat-person">{item.avatarUrl ? <img src={item.avatarUrl} alt={`${item.author} profile`} /> : <i style={{ backgroundColor: item.color }}>{item.author.slice(0, 1).toUpperCase()}</i>}<div><b>{item.author}</b><p>{item.body}</p></div></div><div className="room-chat-reactions" aria-label={`Reactions for ${item.author}'s message`}>{reactionOptions.map(emoji => { const count = item.reactions?.[emoji]?.length ?? 0; const active = Boolean(currentIdentityId && item.reactions?.[emoji]?.includes(currentIdentityId)); return <button type="button" key={emoji} className={active ? "active" : ""} onClick={() => react(item.id, emoji)} aria-label={`React ${emoji}`}><span>{emoji}</span>{count ? <b>{count}</b> : null}</button>; })}<span className="room-reaction-hint"><SmilePlus size={12} />React</span></div></article>) : <p className="live-empty">Messages are replicated in the same encrypted Yjs document.</p>}</div><form onSubmit={event => { event.preventDefault(); send(message); setMessage(""); }}><input value={message} onChange={event => setMessage(event.target.value)} placeholder="Say something to the room" /><button aria-label="Send room chat"><Send size={15} /></button></form></div></div></section>;
}
