import type { PeerPresence } from "@/features/editor/usePeerDocument";
import { BadgeCheck, ShieldCheck, X } from "lucide-react";

export function CollaboratorProfileSheet({ person, onClose }: { person: PeerPresence; onClose: () => void }) {
  return <div className="collaborator-profile-backdrop" role="presentation" onMouseDown={onClose}><section className="collaborator-profile-sheet" role="dialog" aria-modal="true" aria-label={`${person.name}'s profile`} onMouseDown={event => event.stopPropagation()}><button className="collaborator-profile-close" onClick={onClose} aria-label="Close profile"><X size={18} /></button><div className="collaborator-profile-avatar">{person.avatarUrl ? <img src={person.avatarUrl} alt={`${person.name} profile`} /> : <i style={{ backgroundColor: person.color }}>{person.name.slice(0, 1).toUpperCase()}</i>}</div><p className="eyebrow">ROOM COLLABORATOR</p><h2>{person.name}</h2><p className="collaborator-profile-status">{person.verified ? <><BadgeCheck size={15} />Verified PeerLock account</> : <><ShieldCheck size={15} />Approved room collaborator</>}</p><p className="collaborator-profile-copy">This profile is visible because you are both approved members of this encrypted room. Email, documents, chat history, room secrets, and other private account data are not shown here.</p></section></div>;
}
